<% layout('layouts/boilerplate') %>

<div class="page-heading">
    <div>
        <h1>Watchlist</h1>
        <p>Shifts you saved for later.</p>
    </div>
</div>

<% if (wishlistListings.length > 0) { %>
    <ul class="listing-grid">
        <% wishlistListings.forEach(listing => { %>
            <li class="listing-card">
                <a href="/listings/<%= listing._id %>">
                    <img src="<%= listing.image.url %>" alt="<%= listing.title %>">
                </a>
                <div class="listing-card-body">
                    <h2><a href="/listings/<%= listing._id %>"><%= listing.title %></a></h2>
                    <p><%= listing.description %></p>
                    <p class="price">&#8377; <%= listing.price.toLocaleString('en-IN') %> / hour</p>
                </div>
            </li>
        <% }) %>
    </ul>
<% } else { %>
    <p class="muted">Your watchlist is empty. Browse shifts and tap the heart to save them here.</p>
    <a class="btn" href="/listings">Browse shifts</a>
<% } %>
